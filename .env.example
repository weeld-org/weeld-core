# Application
NODE_ENV=development
PORT=3000

# Postgres (local app -> container)
POSTGRES_USER=weeld
POSTGRES_PASSWORD=weeld
POSTGRES_DB=weeld_core
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Unified connection string (commonly used by ORMs)
DATABASE_URL=postgres://weeld:weeld@localhost:5432/weeld_core

# If the app runs in Docker and connects to the DB container, use this host instead:
# DOCKER_DATABASE_URL=postgres://weeld:weeld@db:5432/weeld_core

